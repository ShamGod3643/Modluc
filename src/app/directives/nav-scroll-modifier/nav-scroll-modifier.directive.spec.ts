import { NavScrollModifierDirective } from './nav-scroll-modifier.directive';

describe('NavScrollModifierDirective', () => {
  it('should create an instance', () => {
    const directive = new NavScrollModifierDirective();
    expect(directive).toBeTruthy();
  });
});
